# customerSupportMCP1
# Customer Support MCP Server  A production-grade AI customer-support server built with **FastMCP**, **LangGraph**, and a **Groq → Ollama** LLM fallback chain.  It exposes six MCP tools consumable by any MCP client (Claude Desktop, VS Code Copilot, custom agents).
